apply plugin: 'war'

repositories {
    mavenCentral()
}

dependencies {
    testCompile(
            [group: 'junit', name: 'junit', version: '4.11+'],
            [group: 'org.hamcrest', name: 'hamcrest-all', version: '1.3+']
    )
}

